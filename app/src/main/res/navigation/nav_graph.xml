<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/movieList">

    <fragment
        android:id="@+id/movieList"
        android:name="com.kinopoisklite.view.MovieList"
        android:label="movie_list_fragment"
        tools:layout="@layout/movie_list_fragment" >
        <action
            android:id="@+id/action_movieList_to_movieAdd"
            app:destination="@id/movieAdd" />
    </fragment>
    <fragment
        android:id="@+id/movieAdd"
        android:name="com.kinopoisklite.view.MovieAdd"
        android:label="movie_add_fragment"
        tools:layout="@layout/movie_add_fragment" />
</navigation>